import { FilterInterface } from '@/components/filters/FilterInterface';

export default function FiltersPage() {
  return (
    <main className="min-h-screen bg-gradient-to-br from-gray-50 to-gray-100 py-8">
      <FilterInterface />
    </main>
  );
}
